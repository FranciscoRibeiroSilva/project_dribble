import 'package:flutter/material.dart';

class HelperCentre extends StatelessWidget {
  const HelperCentre({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
